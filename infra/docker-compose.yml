version: "3.9"

services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env

  worker:
    build:
      context: ../worker
      dockerfile: Dockerfile
    env_file:
      - ../.env
