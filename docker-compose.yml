services:
  agent:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    stdin_open: true
    tty: true
    ports:
      - "7860:7860"
    volumes:
      - ./data:/app/data
